name: LLM Workflow CI

on:
  push:
    branches: [main, 'security/*']
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pre-commit pytest pytest-cov

      - name: Run pre-commit hooks
        run: pre-commit run --all-files

      - name: Run pytest
        run: pytest -v --maxfail=1 --disable-warnings

# Optional: LLM sanity checks are environment-specific and require Ollama
# runners available on the CI host. Uncomment and configure only if you
# have an authenticated Ollama instance accessible from the runner.
#
#      - name: Optional: Run sanity LLM test
#        env:
#          OLLAMA_HOST: http://127.0.0.1:11435
#        run: |
#          ollama ps || true
#          ollama list || true
