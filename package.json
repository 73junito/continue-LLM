{
	"name": "continue-config",
	"version": "1.0.0",
	"description": "My Continue Configuration",
	"main": "config.js",
	"scripts": {
		"validate-config": "node ./scripts/validate-config.js ./config.yaml examples/*.yaml",
		"lint-schema": "node ./scripts/lint-schema.js",
		"prepare": "husky install"
	},
	"dependencies": {
		"ajv": "^8.12.0",
		"js-yaml": "^4.1.0"
	},
	"devDependencies": {
		"husky": "^8.0.0"
	}
}