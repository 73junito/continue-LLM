name: OpenAI Example
version: 1.0.0
schema: v1

models:
  - id: gpt-4
    name: gpt-4
    provider: openai
    model: gpt-4
    apiKey: sk-REPLACE_ME

routes:
  - match: '(assistant|chat)'
    model: gpt-4

defaults:
  timeout_seconds: 120
  max_tokens: 8192
